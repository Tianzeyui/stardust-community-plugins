/**
 * inspiration — 灵感插件（新范式）
 *
 * HOST 半端：无工具（纯页面插件）
 * CLIENT 半端：挂载宿主内置 InspirationPage
 */
export const name = 'inspiration'
export const inject = []
export const provide = []

export function apply() {
  // 纯页面插件，无工具
}
